[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Route all requests to index.html so TanStack Router handles navigation client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
